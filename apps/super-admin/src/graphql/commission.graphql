# Query to get the current commission setting
query Commission {
  commission {
    # Remove wrapper fields if 'commission' directly returns the Commission type
    # statusCode
    # success
    # message
    # data {
    id
    percentage
    createdAt
    # updatedAt # Removed if not needed
    # }
  }
}

# Mutation to update the commission percentage
mutation UpdateCommission($percentage: Float!) {
  updateCommission(percentage: $percentage) {
    # Remove wrapper fields if 'updateCommission' directly returns the Commission type
    # statusCode
    # success
    # message
    # data {
    id
    percentage
    createdAt
    # updatedAt # Removed if not needed
    # }
  }
}
