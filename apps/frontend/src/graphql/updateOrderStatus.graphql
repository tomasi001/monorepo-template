# Mutation to update order status
mutation UpdateOrderStatus($id: ID!, $status: String!) {
  # Use ID! type for id
  updateOrderStatus(id: $id, status: $status) {
    # Expect Order type directly, no wrapper
    id
    status
  }
}
