query Order($id: String!) {
  order(id: $id) {
    statusCode
    success
    message
    data {
      id
      total
      status
      items {
        menuItem {
          name
          price
        }
        quantity
      }
      payment {
        amount
        status
      }
    }
  }
}
