# Frontend mutation to initialize a Paystack transaction
mutation InitializeTransaction($input: InitializeTransactionInput!) {
  initializeTransaction(input: $input) {
    authorizationUrl
    accessCode
    reference
  }
}
